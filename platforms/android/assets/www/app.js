'use strict';

// Declare app level module which depends on views, and components
angular.module('cmaManagementApp', [
    'ui.bootstrap',
    //'nativeUtil',
    'routerConfigModule',
    'ngResource'
]);